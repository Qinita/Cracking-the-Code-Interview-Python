#Data Structure
#Arrays and Strings
#1. Implement an algorithm to determine if a string has all 
#unique characters. What if you cannot use additional data
#structure?

def IsUnique(s):
    if all(s.count(x)==1 for x in s):
        return 'Yes'
           
    else:
        return 'No'
